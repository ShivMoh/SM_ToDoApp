const headers = {
    'Content-Type': 'text/html',
    'Access-Control-Allow-Origin': '*',
    'Access-Control-Allow-Headers': '*'
}

module.exports = {
    SERVER_PORT: 3000,
    HTTP_HEADERS: headers
};